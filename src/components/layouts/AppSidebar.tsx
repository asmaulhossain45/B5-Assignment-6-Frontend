import { Sidebar, SidebarHeader } from "../ui/sidebar";

const AppSidebar = () => {
  return (
    <Sidebar>
      <SidebarHeader>
        <h1>App Sidebar</h1>
      </SidebarHeader>
    </Sidebar>
  );
};

export default AppSidebar;
